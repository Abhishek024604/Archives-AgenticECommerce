# API URL for Frontend Deployment
# When developing locally, this can be empty (defaults to http://localhost:5000 in code)
# When deploying to production (e.g. Vercel), set this to your deployed backend URL.
VITE_API_URL=
